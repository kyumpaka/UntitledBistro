--------------------- ?�이�? ?�체??�� -------------------------------------
DROP TABLE EmpRegister CASCADE CONSTRAINTS;
DROP TABLE Salary CASCADE CONSTRAINTS;
DROP TABLE Schedule CASCADE CONSTRAINTS;
---------------------------------------------------------------------------
CREATE TABLE EMPREGISTER ( -- 직원?�록 ?�이�?
        EMPREGISTER_NAME VARCHAR2 (50) NOT NULL, -- ?�름
        EMPREGISTER_PHOTO VARCHAR2 (300), -- ?�원 ?�진 
        EMPREGISTER_EMPNUM VARCHAR2(20),  -- ?�번(PK)
        EMPREGISTER_AGE NUMBER, -- ?�이
        EMPREGISTER_TEL VARCHAR2 (50), -- ?�화번호 
        EMPREGISTER_JUMIN VARCHAR2 (50), -- 주�?1 
        EMPREGISTER_JUMIN2 VARCHAR2 (50),  -- 주�?2
        EMPREGISTER_GENDER VARCHAR2 (50), -- ?�별
        EMPREGISTER_ADDR VARCHAR2 (100), -- 거주지
        EMPREGISTER_ADDR2 VARCHAR2 (100), -- 거주지
        EMPREGISTER_ADDR3 VARCHAR2 (100), -- 거주지
        EMPREGISTER_ADDR4 VARCHAR2 (100), -- 거주지
        EMPREGISTER_GRADE VARCHAR2 (50),  -- 직급 
        EMPREGISTER_BANKNUM VARCHAR2 (100), -- ?�장번호 
        EMPREGISTER_BANKNAME VARCHAR2 (50), -- ?�?�명 
        EMPREGISTER_ACCOUNTHOLDER VARCHAR2(50), -- ?�금�?  
        EMPREGISTER_ENTRYDAY DATE DEFAULT SYSDATE, -- ?�사?�짜 
        EMPREGISTER_PAYCLASSFIY VARCHAR2(20), -- 급여구분
        EMPREGISTER_PAYTIME VARCHAR2(20), -- ?�급        
        EMPREGISTER_PAYDAY VARCHAR2(20), -- ?�급
        EMPREGISTER_WORKPLAN NUMBER, -- 근무?�정?�간
        EMPREGISTER_LEAVECOMPANY VARCHAR2(20), -- ?�사?��? 
        EMPREGISTER_ENABLED NUMBER DEFAULT 1, -- ?�용?��?
        EMPREGISTER_AUTHORITY VARCHAR2(20) DEFAULT 'ROLE_MEMBER'-- 권한
);
ALTER TABLE EMPREGISTER ADD CONSTRAINT PK_EMPREGISTER_EMPNUM PRIMARY KEY(EMPREGISTER_EMPNUM);

INSERT INTO EMPREGISTER(EMPREGISTER_NAME, EMPREGISTER_EMPNUM, EMPREGISTER_JUMIN, EMPREGISTER_ENABLED, EMPREGISTER_AUTHORITY) 
VALUES('알바', 'MEMBER', '12345', 1, 'ROLE_MEMBER');
INSERT INTO EMPREGISTER(EMPREGISTER_NAME, EMPREGISTER_EMPNUM, EMPREGISTER_JUMIN, EMPREGISTER_ENABLED, EMPREGISTER_AUTHORITY) 
VALUES('매니저', 'MANAGER', '12345', 1, 'ROLE_MANAGER');
INSERT INTO EMPREGISTER(EMPREGISTER_NAME, EMPREGISTER_EMPNUM, EMPREGISTER_JUMIN, EMPREGISTER_ENABLED, EMPREGISTER_AUTHORITY) 
VALUES('사장', 'ADMIN', '12345', 1, 'ROLE_ADMIN');

CREATE TABLE SALARY ( -- 근무 �? 급여?�역, ?�?�하?�테?�블
        SALARY_NO VARCHAR2(20), -- 번호(PK)
        SALARY_EMPREGISTER_EMPNUM VARCHAR2(20), -- ?�번(FK)
        SALARY_WORKINGTIME NUMBER, -- ?�한?�간
        SALARY_WORKINGDATE NUMBER, -- 근무?�수
        SALARY_HOLLYDAYSTART DATE, -- ?��??�작
        SALARY_HOLLYDAYEND DATE, -- ?��?종료
        SALARY_PAY  NUMBER--급여계산??
);

CREATE TABLE  SCHEDULE ( -- ?��?�?
        SCHEDULE_NO VARCHAR2(20), -- 번호(PK)
        SCHEDULE_EMPREGISTER_EMPNUM VARCHAR2(20),   -- ?�번(FK)
        SCHEDULE_WORKINGSTART DATE, -- 출근?�간
        SCHEDULE_WORKINGEND DATE, -- ?�근?�간
        SCHEDULE_WORKINGTIME NUMBER, -- ?�한?�간 (매달 초기??)
        SCHEDULE_WORKINGDATE NUMBER -- ?�월근무?�수 (?�근 카운??, 매달 초기??)
);

COMMIT;